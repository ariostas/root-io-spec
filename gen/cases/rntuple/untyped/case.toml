id   = "rntuple/untyped"
file = "data/rntuple/untyped.root"

description = """
Untyped collections and records: fields with a structural role and **no type
name**.

No C++ type produces one. `fRecord` is an `RRecordField` built from two member
fields rather than from a class, and `fColl` comes from
`RVectorField::CreateUntyped`; both are what the document's *Untyped collections
and records* section describes, and the claim it makes that a file can show is
the one asserted below — the type name of each parent is a string of length
**0**, while its members and its item field are ordinary typed fields.

The rest follows the typed shapes exactly: the untyped record has no columns, like
a class; the untyped collection has an index column, like a `std::vector`. So the
only thing that distinguishes an untyped field on disk is the empty string, which
is worth knowing before writing a reader that keys off the type name.
"""

# The same standard-library difference as the other RNTuple cases: the
# ROOT::RNTuple anchor's streamer info is recorded with a different fType and type
# name under libstdc++. ElementTypes.md 2.4, PLAN.md 9.6.
digest        = false
digest_reason = """
ROOT::RNTuple's seven std::uint64_t members are recorded as fType 17 `ULong64_t`
with libc++ and fType 14 `unsigned long` with libstdc++ -- a 28-byte LENGTH
difference a mask cannot undo. The byte assertions still run. ElementTypes.md 2.4
"""

[[bytes]]
name = "the header envelope preamble: type 1, length 424"
offset = 270
type = "u64le"
value = 27787265

[[bytes]]
name = "fRecord: structural role 0x02 (record)"
offset = 350
type = "u16le"
value = 0x02

[[bytes]]
name = "fRecord: its type name is empty, a string of length 0"
offset = 365
type = "u32le"
value = 0

[[bytes]]
name = "fX: structural role 0x00 (plain)"
offset = 397
type = "u16le"
value = 0x00

[[bytes]]
name = "fX: its type name is 'float', a string of length 5"
offset = 407
type = "u32le"
value = 5

[[bytes]]
name = "fX: column type Real32 = 0x0c"
offset = 590
type = "u16le"
value = 0x0c

[[bytes]]
name = "fN: structural role 0x00 (plain)"
offset = 444
type = "u16le"
value = 0x00

[[bytes]]
name = "fN: its type name is 'std::int32_t', a string of length 12"
offset = 454
type = "u32le"
value = 12

[[bytes]]
name = "fN: column type Int32 = 0x07"
offset = 610
type = "u16le"
value = 0x07

[[bytes]]
name = "fColl: structural role 0x01 (collection)"
offset = 498
type = "u16le"
value = 0x01

[[bytes]]
name = "fColl: its type name is empty, a string of length 0"
offset = 511
type = "u32le"
value = 0

[[bytes]]
name = "fColl: column type Index64 = 0x0f"
offset = 630
type = "u16le"
value = 0x0f

[[bytes]]
name = "_0: structural role 0x00 (plain)"
offset = 543
type = "u16le"
value = 0x00

[[bytes]]
name = "_0: its type name is 'float', a string of length 5"
offset = 553
type = "u32le"
value = 5

[[bytes]]
name = "_0: column type Real32 = 0x0c"
offset = 650
type = "u16le"
value = 0x0c
